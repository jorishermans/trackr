import { vibrato, VibratoOptions } from './vibrato';
import { envelope, EnvelopeOptions } from './envelope';
import { gain, GainOptions, randomGain } from './gain';
export { 
    gain,
    randomGain,
    envelope,
    vibrato,
    EnvelopeOptions,
    GainOptions,
    VibratoOptions
}