import { envelope, EnvelopeOptions } from './envelope';
import { gain, GainOptions, randomGain } from './gain';
export { 
    gain,
    randomGain,
    envelope,
    EnvelopeOptions,
    GainOptions 
}